spring:
  cloud:
    config:
      uri: http://localhost:8111


management:
  endpoints:
    web:
      exposure:
        include: "*"
      expose: "*"

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka